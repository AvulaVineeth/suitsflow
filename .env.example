# Application
SUITSFLOW_APP_NAME=SuitsFlow
SUITSFLOW_ENVIRONMENT=local
SUITSFLOW_DEBUG=true
SUITSFLOW_API_V1_PREFIX=/api/v1
SUITSFLOW_LOG_LEVEL=INFO

# Database
SUITSFLOW_DATABASE_URL=postgresql+asyncpg://suitsflow:suitsflow@localhost:5432/suitsflow
SUITSFLOW_DATABASE_TIMEOUT_SECONDS=3

# Optional S3 upload storage (existing private bucket; standard AWS credential chain)
# SUITSFLOW_S3_BUCKET=<private bucket name>
# Required for local/test S3: explicitly select your personal AWS profile.
# SUITSFLOW_S3_PROFILE=suitsflow-personal
# Required whenever S3 is enabled: your bucket owner's 12-digit AWS account ID.
# SUITSFLOW_S3_EXPECTED_BUCKET_OWNER=<personal account ID>
SUITSFLOW_S3_REGION=us-east-1
SUITSFLOW_UPLOAD_TIMEOUT_SECONDS=120

# Local development identity (disabled by default; never enable outside local/test)
SUITSFLOW_DEVELOPMENT_AUTH_ENABLED=false
# SUITSFLOW_DEVELOPMENT_AUTH_TOKEN=<generate a random token of at least 32 characters>
# SUITSFLOW_DEVELOPMENT_USER_ID=<existing active user uuid in the selected tenant>
# SUITSFLOW_DEVELOPMENT_TENANT_ID=<existing active tenant uuid>

# Optional trusted private ClamAV daemon; unset means scanning is disabled.
# SUITSFLOW_CLAMAV_HOST=127.0.0.1
SUITSFLOW_CLAMAV_PORT=3310
SUITSFLOW_CLAMAV_TIMEOUT_SECONDS=120
# Must not exceed the daemon's MaxFileSize/MaxScanSize settings.
SUITSFLOW_CLAMAV_MAX_FILE_BYTES=104857600
SUITSFLOW_CLAMAV_MAX_SCAN_BYTES=314572800
